import { Injectable } from '@angular/core';
import { Employee } from 'src/state/employee/employee.interface';

@Injectable()
export class EmployeeListService {
  filterEmployees(employees: Array<Employee>) {
    employees;
  }
}
